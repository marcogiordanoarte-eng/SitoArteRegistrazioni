PORT=3001
# We'll open the browser ourselves to the right route (/sounds)
BROWSER=none

# Disable creation of source maps to suppress noisy missing source map warnings from music-metadata & other deps
GENERATE_SOURCEMAP=false
