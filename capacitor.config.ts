import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arteregistrazioni.app',
  appName: 'ArteRegistrazioni',
  webDir: 'build'
};

export default config;
